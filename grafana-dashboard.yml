---
- hosts: all
  become: yes
  tasks:
  - name: Add dashboard
    ansible.builtin.uri:
      url: http://admin:admin@localhost:3000/api/dashboards/db
      method: POST
      src: "{\"dashboard\":$(cat dashboard.json.j2)}"
      remote_src: yes
